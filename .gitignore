__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
data/raw/
.venv/
venv/
.idea/
.vscode/

# secrets, never committed
.env
*.env
*_api_key.txt
*.key
secrets/

# the full feature panel is rebuilt by scripts/download_data.py; too large to version monthly
data/feature_panel.parquet

# the point in time feature panel is rebuilt by scripts/build_pit_universe.py
# feature panels and merged prices are rebuilt by scripts/build_pit_universe.py
data/pit/feature_panel_pit*.parquet
data/pit/prices_pit*.parquet

# large rebuilt artifacts and the Kronos clone
kronos/
data/pit/prices_pit.parquet
contestants/kronos_pit_preds.parquet

# strategy and positioning notes, never published
docs/_private/

leaderboard/*.log
leaderboard/overnight_chain.log
